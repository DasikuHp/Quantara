# Dependencias de Quantara (runtime mínimo)
# El motor OCR es PaddleOCR (ligero, CPU). NO se usan torch/transformers/Donut
# en el camino activo, por eso no figuran aquí: ahorran ~2-3 GB de instalación.

# API & servidor
fastapi
uvicorn
python-multipart
pydantic

# OCR
paddleocr
paddlepaddle
pdf2image
pillow
numpy

# Persistencia (SQLite, incluido en la stdlib de Python)
sqlalchemy
